<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
<title>Proyección de Patrimonio — Pauli & Jordi</title>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.js"></script>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    background: #f5f5f3;
    color: #1a1a1a;
    padding: 20px 16px 40px;
  }

  .container { max-width: 480px; margin: 0 auto; }

  h1 { font-size: 24px; font-weight: 600; margin-bottom: 3px; letter-spacing: -0.3px; }
  .subtitle { font-size: 14px; color: #666; margin-bottom: 6px; line-height: 1.4; }
  .meta { font-size: 11px; color: #aaa; line-height: 1.6; margin-bottom: 20px; }

  .highlight {
    background: #e8f5ee;
    border-radius: 14px;
    padding: 16px;
    margin-bottom: 20px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 14px;
  }
  .hl-item-full { grid-column: 1 / -1; border-bottom: 0.5px solid #c8e6d4; padding-bottom: 12px; margin-bottom: 2px; }
  .hl-label { font-size: 11px; color: #3B6D11; margin-bottom: 2px; text-transform: uppercase; letter-spacing: 0.3px; }
  .hl-val { font-size: 22px; font-weight: 600; color: #1e4d09; letter-spacing: -0.5px; }
  .hl-val-sm { font-size: 18px; font-weight: 600; color: #27500A; }

  .year-selector {
    display: flex;
    gap: 8px;
    margin-bottom: 16px;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
  }
  .year-selector::-webkit-scrollbar { display: none; }
  .year-btn {
    flex-shrink: 0;
    padding: 8px 22px;
    border-radius: 20px;
    border: 1.5px solid #ddd;
    background: white;
    font-size: 14px;
    font-weight: 500;
    color: #666;
    cursor: pointer;
    transition: all 0.15s;
  }
  .year-btn.active { background: #1a1a1a; border-color: #1a1a1a; color: white; }

  .main-card {
    background: white;
    border-radius: 16px;
    padding: 20px;
    margin-bottom: 14px;
    border: 0.5px solid #e5e5e5;
    box-shadow: 0 1px 4px rgba(0,0,0,0.04);
  }
  .main-card-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 18px; }
  .main-total { font-size: 38px; font-weight: 600; letter-spacing: -1px; line-height: 1; }
  .main-growth { font-size: 13px; font-weight: 600; padding: 4px 10px; border-radius: 20px; margin-top: 4px; }
  .growth-pos { background: #e8f5ee; color: #27500A; }
  .growth-neg { background: #fdecea; color: #b71c1c; }

  .breakdown { display: flex; flex-direction: column; gap: 8px; }
  .breakdown-row { display: flex; justify-content: space-between; align-items: center; padding: 10px 12px; border-radius: 10px; background: #fafafa; }
  .breakdown-left { display: flex; align-items: center; gap: 10px; }
  .breakdown-dot { width: 10px; height: 10px; border-radius: 3px; flex-shrink: 0; }
  .breakdown-label { font-size: 13px; color: #555; }
  .breakdown-val { font-size: 14px; font-weight: 600; color: #1a1a1a; }

  .divider { border: none; border-top: 0.5px solid #eee; margin: 16px 0; }

  .extracted-title { font-size: 11px; color: #aaa; text-transform: uppercase; letter-spacing: 0.4px; margin-bottom: 10px; }
  .extracted-row { display: flex; justify-content: space-between; font-size: 13px; margin-bottom: 6px; color: #666; }
  .extracted-row.total { font-weight: 600; color: #1a1a1a; font-size: 14px; }
  .extracted-val { font-weight: 600; color: #27500A; }

  .chart-card {
    background: white;
    border-radius: 16px;
    padding: 18px;
    margin-bottom: 12px;
    border: 0.5px solid #e5e5e5;
    box-shadow: 0 1px 4px rgba(0,0,0,0.04);
  }
  .chart-title { font-size: 13px; font-weight: 600; margin-bottom: 12px; line-height: 1.3; }
  .legend { display: flex; flex-wrap: wrap; gap: 6px 12px; margin-bottom: 12px; }
  .legend-item { display: flex; align-items: center; gap: 5px; font-size: 11px; color: #666; }
  .legend-dot { width: 9px; height: 9px; border-radius: 2px; flex-shrink: 0; }

  .note { font-size: 11px; color: #bbb; margin-top: 16px; line-height: 1.8; padding: 14px; background: white; border-radius: 12px; border: 0.5px solid #eee; }
  .footer { margin-top: 24px; font-size: 11px; color: #ccc; text-align: center; }

  @media(min-width: 600px) {
    body { padding: 40px 24px; }
    .container { max-width: 900px; }
    h1 { font-size: 28px; }
    .highlight { grid-template-columns: repeat(3, 1fr); }
    .hl-item-full { grid-column: auto; border-bottom: none; padding-bottom: 0; margin-bottom: 0; }
  }
</style>
</head>
<body>
<div class="container">

  <h1>Proyección de Patrimonio</h1>
  <div class="subtitle">Pauli &amp; Jordi ❤️ — Escenario de capitalización inmobiliaria</div>
  <div class="meta">Capital inicial €4,000,000 · Bot crypto Binance · Banca privada · Casa Mallorca · Abril 2026</div>

  <div class="highlight">
    <div class="hl-item-full">
      <div class="hl-label">Extracción mensual total</div>
      <div class="hl-val">€26,285/mes</div>
    </div>
    <div>
      <div class="hl-label">Gastos personales</div>
      <div class="hl-val-sm">€20,000</div>
    </div>
    <div>
      <div class="hl-label">Hipoteca Mallorca</div>
      <div class="hl-val-sm">€6,285</div>
    </div>
  </div>

  <div class="year-selector" id="yearSelector"></div>
  <div class="main-card" id="mainCard"></div>

  <div class="chart-card">
    <div class="chart-title">Patrimonio acumulado por componente (€M)</div>
    <div class="legend">
      <span class="legend-item"><span class="legend-dot" style="background:#1D9E75"></span>Bot crypto</span>
      <span class="legend-item"><span class="legend-dot" style="background:#378ADD"></span>Banca privada</span>
      <span class="legend-item"><span class="legend-dot" style="background:#7F77DD"></span>Mallorca equity</span>
      <span class="legend-item"><span class="legend-dot" style="background:#B4B2A9"></span>Cash</span>
    </div>
    <div style="position:relative;height:220px;"><canvas id="chart1"></canvas></div>
  </div>

  <div class="chart-card">
    <div class="chart-title">Extracción mensual fija (€)</div>
    <div class="legend">
      <span class="legend-item"><span class="legend-dot" style="background:#1D9E75"></span>Gastos personales €20,000</span>
      <span class="legend-item"><span class="legend-dot" style="background:#7F77DD"></span>Hipoteca €6,285</span>
    </div>
    <div style="position:relative;height:80px;"><canvas id="chart2"></canvas></div>
  </div>

  <div class="note">Supuestos: capital inicial €4M · bot crypto 3% mensual neto en Binance · banca privada 7% anual · reserva cash €600k sin tocar · extracción €20,000/mes gastos personales + €6,285/mes hipoteca · hipoteca 100% sobre €1.4M a 30 años al 3.5% fijo · inmueble Mallorca revalorizado al 3% anual · proyección orientativa, no constituye asesoramiento financiero.</div>
  <div class="footer">Pauli &amp; Jordi ❤️ · Documento interno · Abril 2026</div>

</div>
<script>
const MORT_PAYMENT = 6285, PERSONAL = 20000;
const TOTAL_EXTRACT = MORT_PAYMENT + PERSONAL;
const MALLORCA_INITIAL = 1400000;
const MORT_RATE = 0.035 / 12;
const INIT_TOTAL = 4000000 + MALLORCA_INITIAL;

function calcMortgageBalance(months) {
  let bal = 1400000;
  for (let i = 0; i < months; i++) {
    const interest = bal * MORT_RATE;
    bal -= (MORT_PAYMENT - interest);
    if (bal < 0) bal = 0;
  }
  return bal;
}

function calc(years) {
  let bot = 1700000, banca = 1700000;
  const months = years * 12;
  for (let m = 0; m < months; m++) {
    bot = bot * 1.03 - TOTAL_EXTRACT;
    banca = banca * (1 + 0.07 / 12);
  }
  const mallorcaValue = MALLORCA_INITIAL * Math.pow(1 + 0.03/12, months);
  const mallorcaEquity = Math.max(mallorcaValue - calcMortgageBalance(months), 0);
  bot = Math.max(bot, 0);
  return { bot, banca, cash: 600000, mallorcaEquity, total: bot + banca + 600000 + mallorcaEquity };
}

const years = [1, 3, 5, 10];
const data = years.map(y => ({ year: y, ...calc(y) }));
let activeYear = 0;

function fmt(n) { return '€' + (n/1e6).toFixed(2) + 'M'; }
function fmtK(n) { return '€' + (n/1e3).toFixed(0) + 'k'; }

function renderCard(idx) {
  const d = data[idx];
  const growth = ((d.total - INIT_TOTAL) / INIT_TOTAL * 100).toFixed(0);
  const te = d.year*12*TOTAL_EXTRACT, tp = d.year*12*PERSONAL, tm = d.year*12*MORT_PAYMENT;
  const gc = +growth >= 0 ? 'growth-pos' : 'growth-neg';
  const gs = +growth >= 0 ? '+' : '';
  document.getElementById('mainCard').innerHTML = `
    <div class="main-card-header">
      <div>
        <div style="font-size:11px;color:#aaa;text-transform:uppercase;letter-spacing:0.4px;margin-bottom:6px">Año ${d.year}</div>
        <div class="main-total">${fmt(d.total)}</div>
      </div>
      <div class="main-growth ${gc}">${gs}${growth}%</div>
    </div>
    <div class="breakdown">
      <div class="breakdown-row"><div class="breakdown-left"><span class="breakdown-dot" style="background:#1D9E75"></span><span class="breakdown-label">Bot crypto</span></div><span class="breakdown-val">${fmt(d.bot)}</span></div>
      <div class="breakdown-row"><div class="breakdown-left"><span class="breakdown-dot" style="background:#378ADD"></span><span class="breakdown-label">Banca privada</span></div><span class="breakdown-val">${fmt(d.banca)}</span></div>
      <div class="breakdown-row"><div class="breakdown-left"><span class="breakdown-dot" style="background:#7F77DD"></span><span class="breakdown-label">Mallorca (equity)</span></div><span class="breakdown-val" style="color:#185FA5">${fmt(d.mallorcaEquity)}</span></div>
      <div class="breakdown-row"><div class="breakdown-left"><span class="breakdown-dot" style="background:#B4B2A9"></span><span class="breakdown-label">Cash reserva</span></div><span class="breakdown-val">€0.60M</span></div>
    </div>
    <hr class="divider">
    <div class="extracted-title">Lo que habéis vivido en ${d.year} año${d.year>1?'s':''}</div>
    <div class="extracted-row total"><span>Total extraído</span><span class="extracted-val">${fmtK(te)}</span></div>
    <div class="extracted-row"><span>— Gastos personales</span><span class="extracted-val">${fmtK(tp)}</span></div>
    <div class="extracted-row"><span>— Hipoteca pagada</span><span class="extracted-val">${fmtK(tm)}</span></div>
  `;
}

function renderButtons() {
  document.getElementById('yearSelector').innerHTML = years.map((y,i) =>
    `<button class="year-btn ${i===activeYear?'active':''}" onclick="setYear(${i})">Año ${y}</button>`
  ).join('');
}

function setYear(idx) { activeYear=idx; renderButtons(); renderCard(idx); }

renderButtons(); renderCard(0);

new Chart(document.getElementById('chart1'), {
  type: 'bar',
  data: {
    labels: ['Año 1','Año 3','Año 5','Año 10'],
    datasets: [
      {label:'Bot crypto', data:data.map(d=>+(d.bot/1e6).toFixed(2)), backgroundColor:'#1D9E75'},
      {label:'Banca privada', data:data.map(d=>+(d.banca/1e6).toFixed(2)), backgroundColor:'#378ADD'},
      {label:'Mallorca', data:data.map(d=>+(d.mallorcaEquity/1e6).toFixed(2)), backgroundColor:'#7F77DD'},
      {label:'Cash', data:[0.6,0.6,0.6,0.6], backgroundColor:'#B4B2A9'},
    ]
  },
  options: {
    responsive:true, maintainAspectRatio:false,
    plugins:{legend:{display:false}, tooltip:{callbacks:{label:ctx=>` ${ctx.dataset.label}: €${ctx.raw}M`}}},
    scales:{
      x:{stacked:true, ticks:{font:{size:11}}},
      y:{stacked:true, ticks:{callback:v=>'€'+v+'M', font:{size:11}}}
    }
  }
});

new Chart(document.getElementById('chart2'), {
  type:'bar',
  data:{
    labels:['Mensual'],
    datasets:[
      {label:'Gastos personales', data:[20000], backgroundColor:'#1D9E75'},
      {label:'Hipoteca', data:[6285], backgroundColor:'#7F77DD'},
    ]
  },
  options:{
    indexAxis:'y', responsive:true, maintainAspectRatio:false,
    plugins:{legend:{display:false}, tooltip:{callbacks:{label:ctx=>` ${ctx.dataset.label}: €${ctx.raw.toLocaleString('es-ES')}`}}},
    scales:{
      x:{stacked:true, ticks:{callback:v=>'€'+(v/1000)+'k', font:{size:11}}},
      y:{stacked:true, ticks:{font:{size:11}}}
    }
  }
});
</script>
</body>
</html>
